/**
 *
 * Set of form components already connected to the parent form
 * using react-hook-form.
 *
 * You can use this components instead of the simple input
 * inside the {@link @shared/components} package to reducer form
 * boilerplate and increase readability.
 * Just give the component a name that will be used as key in your
 * form values object
 *
 * @packageDocumentation
 */
import FormTextField from './FormTextField';
import FormCheckbox from './FormCheckbox';
import FormSelect from './FormSelect';

export { FormTextField, FormCheckbox, FormSelect };
